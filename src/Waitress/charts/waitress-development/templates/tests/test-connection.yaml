apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "waitress-development.fullname" . }}-test-connection"
  labels:
    {{- include "waitress-development.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "waitress-development.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
