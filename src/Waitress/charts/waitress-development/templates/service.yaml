apiVersion: v1
kind: Service
metadata:
  name: {{ include "waitress-development.fullname" . }}
  labels:
    {{- include "waitress-development.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "waitress-development.selectorLabels" . | nindent 4 }}
